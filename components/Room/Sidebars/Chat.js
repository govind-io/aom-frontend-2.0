import { Grid } from "@mui/material";

export default function Chat() {
  return (
    <Grid
      container
      sx={{
        height: "100%",
        border: "1px solid white",
      }}
    >
      Chat SideBar
    </Grid>
  );
}
