import { Grid } from "@mui/material";

export default function VideoPlayer() {
  return (
    <Grid
      container
      sx={{
        width: "100%",
        height: "100%",
      }}
    ></Grid>
  );
}
